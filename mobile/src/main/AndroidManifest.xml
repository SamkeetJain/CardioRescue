<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elementrix.cardiorescue">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity" />
        <activity android:name=".UI.ProfileInfo" />
        <activity android:name=".UI.EmergencyContacts" />
        <activity android:name=".UI.AboutUs" />
        <activity android:name=".UI.HowItWorks" />
        <activity android:name=".UI.Statistics" />

        <service android:name=".UI.Srvices"></service>

        <service android:name=".HeartSensor.DataLayerListenerService" >
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.BIND_LISTENER" />
            </intent-filter>
        </service>

        <activity android:name=".UI.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".GPS.GPSService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".UI.Alert" />
        <activity android:name=".UI.AddAddressActivity"
            android:label=""
            android:theme="@style/Theme.AppCompat.Light.Dialog">

        </activity>
    </application>

</manifest>